/*
 * {{fn}}
 */

'use strict';

module.exports = function(ngModule) {

    ngModule.directive('{{name}}', function() {
        return {
            restrict: 'AE',
            link: function(scope, element, attrs) {
                // code goes here
            }
        };

    });

};