angular-init
============

* Koa
* gulp
* browserify
* stylus
* angular

#### Install
  ```$ clone && npm install && bower install && npm install gulp -g```

#### Run
  ```$ gulp ```

#### Test
  ```$ gulp karma```
